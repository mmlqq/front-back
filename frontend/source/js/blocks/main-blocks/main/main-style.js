import styled from 'styled-components';

import { DEFAULT_PADDING } from '../../styles-data/variables';

const Main = styled.main`
  padding: 0 ${DEFAULT_PADDING};
  flex: 1 0 auto;
`;

export default Main;
