import styled from 'styled-components';

import { MARGIN_AUTO } from '../../styles-data/variables';

const Svg = styled.svg`
  margin: ${MARGIN_AUTO};
  display: block;
  width: 50px;
  height: 50px;
`;

export default Svg;
