import styled from 'styled-components';

import { DEFAULT_PADDING } from '../../universal/styles-data/variables';

const Footer = styled.footer`
  padding: 15px ${DEFAULT_PADDING};
`;

export default Footer;
