import styled from 'styled-components';

import { MARGIN_AUTO } from '../../../../../universal/styles-data/variables';

const Section = styled.section`
  margin: ${MARGIN_AUTO}
`;

export default Section;
